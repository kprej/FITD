$input a_position, a_color0
$output v_color0

#include "bgfx_shader.sh"

void main()
{
	gl_Position = mul(u_modelViewProj, vec4(a_position.x/100, a_position.y/100, a_position.z/100, 1.0) );
    v_color0 = a_color0;
}
